spring.application.name=ChatApp

# MongoDB Atlas Configuration
# Use environment variable for production, fallback to Atlas URI for development
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://danciju:ju9QsKqnO4WAh6Es@charappcluster.pdvy98o.mongodb.net/chatapp?retryWrites=true&w=majority&appName=CharAppCluster}

# Server Configuration  
server.address=0.0.0.0
server.port=${PORT:8080}

# CORS Configuration for production
cors.allowed.origins=${ALLOWED_ORIGINS:http://localhost:3000, https://realchatapp-uepa.onrender.com}