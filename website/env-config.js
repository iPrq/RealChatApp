// Frontend Environment Configuration
// You can change these values to point to different backend URLs

window.BACKEND_URL = 'https://realchatapp-1-ogou.onrender.com';
window.APP_NAME = 'RealChatApp';

console.log('🌍 Frontend configured to use backend:', window.BACKEND_URL);
